rules:
  # Rule to detect method definitions
  - id: method_detection
    pattern: |
      def $FUNC(...):
        ...
    message: "Semgrep found a method definition: $FUNC"
    languages:
      - python
    severity: INFO


