#sample code
# Add a to the current number.
def add(a):
# def _get_next_int ( self, a ) : if a < 5 : return a + 2 elif a > 5 : return a + 2 elif a < 5 : return a + 4 else : return a
    if a > 5:
        return a + 2
    else:
        return a + 4